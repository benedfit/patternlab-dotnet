@using PatternLab.Core.Helpers
@model PatternLab.Core.Models.Pattern	
	<script>
		// handle injection of items from PHP
	    var patternPartial = "@Model.Type.StripOrdinals()-@Model.Name.StripOrdinals()";
		var lineage        = @Model.Lineage;
		var lineageR       = @Model.LineageR;
		var patternState   = "@Model.State";
		var cssEnabled     = @Model.CssEnabled.ToString().ToLower();
	</script>
	